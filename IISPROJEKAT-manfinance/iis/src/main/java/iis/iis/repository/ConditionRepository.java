/*package iis.iis.repository;

import iis.iis.entity.Condition;
import iis.iis.entity.Prostorija;
import iis.iis.entity.Reward;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import iis.iis.entity.Competition;

public interface ConditionRepository extends JpaRepository<Condition,Long> {

    //   @Query("select s from Condition s where s.id = ?1")
   // Condition pronadjipoid(Long l);
}*/
